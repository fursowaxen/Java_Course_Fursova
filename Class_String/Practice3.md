Три двойные кавычки (""") — это текстовый блок (Text Block), появившийся в Java 15. Они позволяют писать многострочные строки без экранирования кавычек и переносов строк, сохраняя форматирование. Например: String json = """ {"name":"John"} """; Это делает код с JSON, HTML, SQL более читаемым.
