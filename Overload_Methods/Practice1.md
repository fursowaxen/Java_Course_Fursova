В цикле for (1) — тип переменной n — Number (родительский класс). Компилятор выбирает метод на этапе компиляции по типу ссылки (Number), а не по реальному типу объекта. Вызывается printNum(Number n) для всех элементов.

В прямых вызовах (2) — компилятор видит конкретный тип аргумента и выбирает наиболее подходящий перегруженный метод:
